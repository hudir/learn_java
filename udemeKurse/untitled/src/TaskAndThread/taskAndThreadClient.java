package TaskAndThread;

public class taskAndThreadClient {

    public void someMethod() {
        // Create an Instance of TaskClass
        TaskClass tast = new TaskClass();

        // Create a thread
        Thread thread = new Thread(task);
    }
}
